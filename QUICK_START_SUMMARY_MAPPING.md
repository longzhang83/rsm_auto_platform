# 摘要字段一对多映射 - 快速参考指南

## 快速开始

### 配置方式

在银行流水列名映射配置中，摘要字段支持以下两种方式：

#### 单列映射（原有方式）
```
summary: "摘要"
```

#### 多列映射（新功能）
```
summary: "字段1/字段2/字段3"
```

## 工作原理

系统按**从左到右**的顺序检查每个字段：

```
summary: "摘要1/摘要2/摘要3"
         ↓      ↓      ↓
    第1优先 第2优先 第3优先
```

**处理流程**:
1. 检查"摘要1" → 如果非空，使用该值 ✅
2. 如果"摘要1"为空 → 检查"摘要2" → 如果非空，使用该值 ✅
3. 如果"摘要2"为空 → 检查"摘要3" → 如果非空，使用该值 ✅
4. 如果全为空 → 返回空字符串

## 实际例子

### 场景：某银行流水有3个摘要字段

**源数据**:
| 日期 | 摘要1 | 摘要2 | 摘要3 | 金额 |
|------|-------|-------|-------|------|
| 2024-01-01 | 工资 | | 其他 | 5000 |
| 2024-01-02 | | 租金 | 其他 | 3000 |
| 2024-01-03 | | | 备注 | 2000 |
| 2024-01-04 | | | | 1000 |

**配置**:
```
summary: "摘要1/摘要2/摘要3"
```

**处理结果**:
| 日期 | 提取的摘要 | 来源字段 |
|------|-----------|---------|
| 2024-01-01 | 工资 | 摘要1（第1优先） |
| 2024-01-02 | 租金 | 摘要2（第2优先） |
| 2024-01-03 | 备注 | 摘要3（第3优先） |
| 2024-01-04 | (空) | 全部为空 |

## 支持的空值类型

系统自动识别并处理以下所有空值类型：

- `None` - Python空值
- `NaN` - 数字NaN值
- `""` - 空字符串
- `"   "` - 仅空格（自动strip）
- `"nan"` - 字符串"nan"

## 常见配置

### 配置1: 备用字段
```
summary: "主摘要/备用摘要"
```
优先使用主摘要，主摘要为空时使用备用摘要。

### 配置2: 多级降级
```
summary: "摘要/说明/备注"
```
先查摘要，再查说明，最后查备注。

### 配置3: 完全兼容原有配置
```
summary: "摘要"
```
继续使用单列配置，与原有方式完全兼容。

## 测试验证

所有功能已通过完整的测试验证：

### 单元测试
```bash
python test_summary_field_mapping.py
```
✅ 10个测试用例，全部通过

### 集成测试
```bash
python test_integration_summary_mapping.py
```
✅ 4个集成测试，全部通过

## 详细文档

更多信息请查看：
- **完整文档**: `docs/SUMMARY_FIELD_MAPPING.md`
- **实现细节**: `IMPLEMENTATION_SUMMARY.md`

## 技术信息

**实现位置**: `src/accounting_voucher_generation/bank_statement_pipeline.py`

**核心函数**:
- `_extract_summary_from_columns()` (第402-453行)
- `standardize_bank_statement_data()` (第550-577行修改)

**向后兼容**: ✅ 完全兼容原有单列配置

## FAQ

**Q: 可以配置超过3个字段吗？**
A: 可以，支持任意数量。例如：`"字段1/字段2/字段3/字段4/字段5"`

**Q: 字段顺序重要吗？**
A: 是的，顺序代表优先级。最重要的字段应该放在最前面。

**Q: 现有的单列配置还能用吗？**
A: 完全可以，无需任何改动。系统完全向后兼容。

**Q: 如果某个字段不存在怎么办？**
A: 系统会自动跳过不存在的字段，继续检查下一个。

**Q: 是否支持其他字段的一对多映射？**
A: 目前仅摘要字段支持一对多映射。其他字段可根据需求后续扩展。

---

**最后更新**: 2024-11-10
**状态**: ✅ 生产就绪
